# This is a simple python program to print a welcome message
# print is a built-in function in python that is used to display output on the console
#single line comment
# multiple line comment
print("welcome to python programming") #inline comment 

# Functions - 3 types
# 1. Built-in functions - These are the functions that are provided by python and can be used directly without any import. Example: print(), input(), len(), etc.
# 2. User-defined functions - These are the functions that are defined by the user to perform a specific task. Example:
# def greet(name):
#     return "Hello, " + name + "!"
# 3. Lambda functions - These are anonymous functions that are defined using the lambda keyword. They are used for short, throwaway functions that are not meant to be reused. Example:
# square = lambda x: x * x


# NORMAL CHARACCTERS VS SPECIAL CHARACTERS
# Normal characters are the characters that can be typed directly on the keyboard. Example: a,  
# Special characters are the characters that cannot be typed directly on the keyboard and are represented by a combination of characters. Example: \n (newline), \t (tab), etc.
print("Hello\nWorld") # \n is a special character that represents a newline

print("hello")
print("world")

print("Hello\tWorld") # \t is a special character that represents a tab

